#ifndef TREE_PRINTER

#include <iostream>
#include "treework.h";

#define TREE_PRINTER

void printTree(tree* node, int level);
void printLeafes(tree* top);
void findNote(tree* top, char value[d_p]);

#endif